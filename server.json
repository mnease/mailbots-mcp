{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.mnease/mailbots-mcp",
  "description": "Multi-account email MCP server for Gmail, IMAP, and JMAP: search, send, drafts, labels.",
  "repository": {
    "url": "https://github.com/mnease/mailbots-mcp",
    "source": "github"
  },
  "version": "0.11.3",
  "packages": [
    {
      "registryType": "npm",
      "identifier": "mailbots-mcp",
      "version": "0.11.3",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "MAILBOTS_MCP_PASSPHRASE",
          "description": "Passphrase used to encrypt IMAP/JMAP credentials at rest. Required for IMAP and JMAP accounts. MAILBOX_MCP_PASSPHRASE is still accepted.",
          "format": "string",
          "isSecret": true,
          "isRequired": false
        }
      ]
    }
  ]
}
